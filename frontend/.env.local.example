# Toute variable préfixée NEXT_PUBLIC_ est incluse dans le bundle JS
# envoyé au navigateur — elle n'est donc JAMAIS un secret. L'URL de
# l'API est publique par nature (visible dans le code source livré au
# client de toute façon).
NEXT_PUBLIC_API_URL="http://localhost:3001/api/v1"
